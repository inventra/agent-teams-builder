{
  "name": "agent-teams-builder-release",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs && npm --prefix plugins/agent-teams-builder test",
    "validate": "claude plugin validate plugins/agent-teams-builder --strict",
    "release:build": "node scripts/build-release.mjs"
  }
}
