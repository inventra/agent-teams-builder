{
  "name": "agent-teams-builder-runtime",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Local MCP runtime for host-native Agent execution in Codex and Claude Code",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "doctor": "node scripts/agent-teams-cli.mjs doctor",
    "dashboard": "node scripts/vixo-agents-dashboard.mjs open"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.0.0"
  }
}
